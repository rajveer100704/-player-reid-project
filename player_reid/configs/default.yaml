MODEL:
  NUM_CLASSES: 500
  FEATURE_DIM: 512
  METRIC_LOSS: "arcface"

LOSS:
  TRI_MARGIN: 0.3
  ARCFACE_SCALE: 30.0
  ARCFACE_MARGIN: 0.5

SOLVER:
  BASE_LR: 0.0003
  IMS_PER_BATCH: 16
  MAX_EPOCHS: 60

DATASETS:
  ROOT_DIR: "data/raw_sports_data"
  TRANSFORM: "standard_reid"

OUTPUT_DIR: "results/"
LOG_PERIOD: 20
EVAL_PERIOD: 5
